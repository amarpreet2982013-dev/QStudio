# Extension guide

An extension implements `SilqExtension` and receives `ExtensionApi` in `activate`. The SDK registers commands, languages, themes, snippets, panels, debuggers, simulators, and hardware providers. The API is intentionally provider-neutral and ready for marketplace discovery metadata.
